---
title: JSON Jet Airways API
subtitle: Real-time flight intelligence, wrapped in comfort.
description: The official developer platform for JSON Jet Airways. Query live flight status by carrier code and flight number with a single REST call.
keywords: flight status api, airline api, json jet airways, rest api, postman
layout: custom
hide-toc: true
hide-nav-links: true
hide-feedback: true
---

<style>{`
  .jj-wrap {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 24px 96px;
  }
  /* Custom panels use their own type stack; markdown sections keep your Fern theme font. */
  .jj-hero, .jj-stat, .jj-board, .jj-field, .jj-close, .jj-kicker, .jj-h2, .jj-lead {
    font-family: ui-sans-serif, -apple-system, "Segoe UI", Inter, Helvetica, sans-serif;
  }

  /* ---------- NAV BAR ---------- */
  .jj-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    flex-wrap: wrap;
    margin-top: 26px;
    padding: 11px 14px 11px 13px;
    border-radius: 16px;
    background: linear-gradient(150deg, rgba(16,28,62,0.94), rgba(10,19,41,0.94));
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 18px 40px -30px rgba(3,8,24,0.9);
    font-family: ui-sans-serif, -apple-system, "Segoe UI", Inter, Helvetica, sans-serif;
  }
  .jj-nav-brand {
    display: inline-flex;
    align-items: center;
    gap: 11px;
    text-decoration: none !important;
  }
  .jj-nav-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border-radius: 9px;
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0.04em;
    color: #06102B;
    background: linear-gradient(96deg, #7DD3FC, #F5B942);
  }
  .jj-nav-name {
    font-size: 15px;
    font-weight: 700;
    letter-spacing: -0.012em;
    color: #E8EDFB;
  }
  .jj-nav-links { display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }
  .jj-nav-link {
    padding: 8px 14px;
    border-radius: 9px;
    font-size: 14px;
    font-weight: 600;
    color: #9FB2D8 !important;
    text-decoration: none !important;
    transition: color .16s ease, background .16s ease;
  }
  .jj-nav-link:hover { color: #FFFFFF !important; background: rgba(255,255,255,0.08); }
  .jj-nav-active { color: #FFFFFF !important; background: rgba(255,255,255,0.10); }
  .jj-nav-cta {
    margin-left: 6px;
    color: #06102B !important;
    background: linear-gradient(96deg, #F5B942, #FFD98A);
    box-shadow: 0 10px 24px -12px rgba(245,185,66,0.9);
  }
  .jj-nav-cta:hover { color: #06102B !important; background: linear-gradient(96deg, #FFD98A, #F5B942); }
  @media (max-width: 760px) {
    .jj-nav { justify-content: center; }
    .jj-nav-name { display: none; }
    .jj-nav-link { padding: 7px 11px; font-size: 13px; }
  }

  /* ---------- HERO ---------- */
  .jj-hero {
    position: relative;
    overflow: hidden;
    border-radius: 28px;
    margin-top: 18px;
    padding: 76px 56px 68px;
    background:
      radial-gradient(1000px 460px at 82% -10%, rgba(34,211,238,0.30), transparent 60%),
      radial-gradient(760px 420px at 6% 108%, rgba(245,185,66,0.22), transparent 62%),
      linear-gradient(140deg, #050B1F 0%, #0B1633 44%, #16225C 100%);
    border: 1px solid rgba(255,255,255,0.09);
    box-shadow: 0 40px 90px -40px rgba(5,11,31,0.85);
  }
  .jj-hero::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.045) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.045) 1px, transparent 1px);
    background-size: 56px 56px;
    mask-image: radial-gradient(700px 400px at 30% 30%, #000 20%, transparent 78%);
    -webkit-mask-image: radial-gradient(700px 400px at 30% 30%, #000 20%, transparent 78%);
    pointer-events: none;
  }
  .jj-hero > * { position: relative; z-index: 1; }

  .jj-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 7px 15px;
    border-radius: 999px;
    background: rgba(255,255,255,0.07);
    border: 1px solid rgba(255,255,255,0.16);
    color: #B9C7EA;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    backdrop-filter: blur(6px);
  }

  .jj-title {
    margin: 26px 0 0;
    font-size: clamp(40px, 6.6vw, 74px);
    line-height: 1.02;
    font-weight: 800;
    letter-spacing: -0.035em;
    color: #FFFFFF;
    max-width: 640px;
  }
  .jj-title span {
    background: linear-gradient(96deg, #7DD3FC 0%, #A5B4FC 42%, #F5B942 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .jj-tagline {
    margin: 20px 0 0;
    font-size: clamp(17px, 2vw, 21px);
    line-height: 1.55;
    color: #A9BADF;
    max-width: 640px;
  }
  .jj-tagline b { color: #F5B942; font-weight: 600; }

  .jj-cta-row { display: flex; flex-wrap: wrap; gap: 14px; margin-top: 38px; }
  .jj-btn {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    padding: 13px 24px;
    border-radius: 12px;
    font-size: 15px;
    font-weight: 650;
    text-decoration: none !important;
    transition: transform .18s ease, box-shadow .18s ease, background .18s ease;
  }
  .jj-btn-primary {
    color: #06102B !important;
    background: linear-gradient(96deg, #F5B942, #FFD98A);
    box-shadow: 0 14px 34px -14px rgba(245,185,66,0.85);
  }
  .jj-btn-ghost {
    color: #DDE6FB !important;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.20);
  }
  .jj-btn:hover { transform: translateY(-2px); }
  .jj-btn-ghost:hover { background: rgba(255,255,255,0.13); }

  .jj-chips { display: flex; flex-wrap: wrap; gap: 9px; margin-top: 34px; }
  .jj-chip {
    padding: 6px 13px;
    border-radius: 8px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: #9FB2D8;
    background: rgba(255,255,255,0.05);
    border: 1px solid rgba(255,255,255,0.12);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  }

  .jj-orbit {
    position: absolute;
    top: 50%;
    right: 30px;
    transform: translateY(-50%);
    width: 340px;
    opacity: .9;
    pointer-events: none;
  }
  .jj-orbit-spin { transform-origin: 150px 150px; animation: jjSpin 46s linear infinite; }
  @keyframes jjSpin { to { transform: rotate(360deg); } }
  @media (max-width: 1080px) { .jj-orbit { display: none; } }

  /* ---------- STATS ---------- */
  .jj-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(190px, 1fr));
    gap: 16px;
    margin-top: -46px;
    padding: 0 22px;
    position: relative;
    z-index: 3;
  }
  .jj-stat {
    padding: 24px 22px;
    border-radius: 18px;
    background: linear-gradient(160deg, #101C3E, #0A1329);
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 22px 46px -28px rgba(3,8,24,0.95);
    transition: transform .2s ease, border-color .2s ease;
  }
  .jj-stat:hover { transform: translateY(-4px); border-color: rgba(125,211,252,0.45); }
  a.jj-stat { display: block; text-decoration: none !important; }
  a.jj-stat:hover { border-color: rgba(245,185,66,0.55); }
  a.jj-stat .jj-stat-l::after {
    content: " ↗";
    color: #F5B942;
    font-weight: 700;
  }
  .jj-stat-v {
    font-size: 30px;
    font-weight: 800;
    letter-spacing: -0.02em;
    background: linear-gradient(96deg, #7DD3FC, #F5B942);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .jj-stat-l {
    margin-top: 5px;
    font-size: 12.5px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #8FA2C9;
    font-weight: 600;
  }

  /* ---------- SECTION SHELL ---------- */
  .jj-sec { margin-top: 84px; }
  .jj-kicker {
    font-size: 12px;
    letter-spacing: 0.20em;
    text-transform: uppercase;
    font-weight: 700;
    color: #6E8BC7;
  }
  .jj-h2 {
    margin: 10px 0 0;
    font-size: clamp(27px, 3.4vw, 38px);
    font-weight: 780;
    letter-spacing: -0.028em;
    line-height: 1.15;
  }
  .jj-lead { margin: 12px 0 0; font-size: 16.5px; line-height: 1.65; color: #7E8CA8; max-width: 720px; }

  /* ---------- DEPARTURE BOARD ---------- */
  .jj-board {
    margin-top: 30px;
    border-radius: 22px;
    overflow: hidden;
    background: linear-gradient(168deg, #08122A, #050B1C);
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 34px 70px -40px rgba(3,8,24,0.9);
  }
  .jj-board-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    flex-wrap: wrap;
    padding: 17px 24px;
    background: rgba(255,255,255,0.035);
    border-bottom: 1px solid rgba(255,255,255,0.08);
  }
  .jj-board-title {
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 13px;
    letter-spacing: 0.20em;
    text-transform: uppercase;
    color: #F5B942;
    font-weight: 700;
  }
  .jj-live { display: inline-flex; align-items: center; gap: 8px; font-size: 12px; color: #9FB2D8; font-weight: 600; letter-spacing: .06em; }
  .jj-dot { width: 8px; height: 8px; border-radius: 50%; background: #34D399; box-shadow: 0 0 0 0 rgba(52,211,153,.7); animation: jjPulse 2s infinite; }
  @keyframes jjPulse {
    0%   { box-shadow: 0 0 0 0 rgba(52,211,153,.65); }
    70%  { box-shadow: 0 0 0 11px rgba(52,211,153,0); }
    100% { box-shadow: 0 0 0 0 rgba(52,211,153,0); }
  }

  .jj-row {
    display: grid;
    grid-template-columns: 1.05fr 1.5fr 0.9fr 0.7fr 1.05fr;
    gap: 12px;
    align-items: center;
    padding: 15px 24px;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 14px;
    color: #D6E1F7;
    transition: background .16s ease;
  }
  .jj-row:hover { background: rgba(125,211,252,0.055); }
  .jj-row:last-child { border-bottom: none; }
  .jj-row-head {
    font-size: 10.5px;
    letter-spacing: 0.19em;
    text-transform: uppercase;
    color: #64789F;
    font-weight: 700;
    padding-top: 13px;
    padding-bottom: 13px;
  }
  .jj-row-head:hover { background: transparent; }
  .jj-flt { color: #FFFFFF; font-weight: 700; letter-spacing: .06em; }
  .jj-route { color: #9FB2D8; }
  .jj-route em { color: #7DD3FC; font-style: normal; font-weight: 700; }
  @media (max-width: 760px) {
    .jj-row { grid-template-columns: 1fr 1fr; row-gap: 6px; }
    .jj-row-head { display: none; }
  }

  .jj-pill {
    display: inline-block;
    padding: 4.5px 11px;
    border-radius: 7px;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.10em;
    border: 1px solid transparent;
  }
  .jj-ontime   { color:#6EE7B7; background: rgba(52,211,153,0.12);  border-color: rgba(52,211,153,0.35); }
  .jj-delayed  { color:#FCD34D; background: rgba(245,185,66,0.12);  border-color: rgba(245,185,66,0.38); }
  .jj-boarding { color:#7DD3FC; background: rgba(34,211,238,0.12);  border-color: rgba(34,211,238,0.35); }
  .jj-landed   { color:#C4B5FD; background: rgba(167,139,250,0.13); border-color: rgba(167,139,250,0.35); }
  .jj-cancel   { color:#FCA5A5; background: rgba(248,113,113,0.12); border-color: rgba(248,113,113,0.35); }

  /* ---------- MAP ---------- */
  .jj-map {
    margin-top: 30px;
    border-radius: 22px;
    padding: 12px;
    background: linear-gradient(150deg, #071129, #0C1839 55%, #060D20);
    border: 1px solid rgba(255,255,255,0.10);
    box-shadow: 0 34px 70px -42px rgba(3,8,24,0.9);
  }
  .jj-map svg { display: block; width: 100%; height: auto; }
  .jj-dash { stroke-dasharray: 7 9; animation: jjDash 2.4s linear infinite; }
  @keyframes jjDash { to { stroke-dashoffset: -32; } }

  /* ---------- FIELD GRID ---------- */
  .jj-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(232px, 1fr));
    gap: 14px;
    margin-top: 28px;
  }
  .jj-field {
    padding: 20px;
    border-radius: 16px;
    background: linear-gradient(165deg, #0F1B3D, #0A1228);
    border: 1px solid rgba(255,255,255,0.09);
    transition: transform .18s ease, border-color .18s ease;
  }
  .jj-field:hover { transform: translateY(-4px); border-color: rgba(245,185,66,0.42); }
  .jj-field-k {
    font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 13.5px;
    font-weight: 700;
    color: #7DD3FC;
    word-break: break-word;
  }
  .jj-field-t {
    display: inline-block;
    margin-top: 8px;
    padding: 2.5px 8px;
    border-radius: 6px;
    font-size: 10.5px;
    letter-spacing: .09em;
    text-transform: uppercase;
    font-weight: 700;
    color: #F5B942;
    background: rgba(245,185,66,0.10);
    border: 1px solid rgba(245,185,66,0.28);
  }
  .jj-field-d { margin-top: 11px; font-size: 13.5px; line-height: 1.55; color: #93A6CC; }

  /* ---------- LIFECYCLE TABLE ---------- */
  .jj-tablewrap {
    margin-top: 28px;
    border-radius: 20px;
    overflow: hidden;
    border: 1px solid rgba(255,255,255,0.10);
    background: linear-gradient(168deg, #08122A, #050B1C);
    box-shadow: 0 30px 62px -40px rgba(3,8,24,0.9);
  }
  .jj-table {
    width: 100%;
    border-collapse: collapse;
    font-family: ui-sans-serif, -apple-system, "Segoe UI", Inter, Helvetica, sans-serif;
    font-size: 14.5px;
  }
  .jj-table th {
    text-align: left;
    padding: 15px 22px;
    font-size: 10.5px;
    letter-spacing: 0.19em;
    text-transform: uppercase;
    font-weight: 700;
    color: #64789F;
    background: rgba(255,255,255,0.035);
    border-bottom: 1px solid rgba(255,255,255,0.08);
  }
  .jj-table td {
    padding: 15px 22px;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    color: #C3D2EE;
    vertical-align: middle;
  }
  .jj-table tr:last-child td { border-bottom: none; }
  .jj-table tbody tr { transition: background .16s ease; }
  .jj-table tbody tr:hover { background: rgba(125,211,252,0.055); }
  .jj-swatch {
    display: inline-block;
    width: 9px; height: 9px;
    border-radius: 50%;
    margin-right: 9px;
    vertical-align: middle;
  }
  .jj-sw-green   { background: #34D399; box-shadow: 0 0 10px rgba(52,211,153,.65); }
  .jj-sw-amber   { background: #F5B942; box-shadow: 0 0 10px rgba(245,185,66,.6); }
  .jj-sw-neutral { background: #94A3B8; }
  .jj-sw-red     { background: #F87171; box-shadow: 0 0 10px rgba(248,113,113,.6); }
  @media (max-width: 700px) {
    .jj-table th:nth-child(2), .jj-table td:nth-child(2) { display: none; }
  }

  /* ---------- CLOSING BANNER ---------- */
  .jj-close {
    margin-top: 84px;
    border-radius: 24px;
    padding: 54px 44px;
    text-align: center;
    background:
      radial-gradient(600px 260px at 50% -20%, rgba(34,211,238,0.26), transparent 65%),
      linear-gradient(140deg, #0A1329, #16225C);
    border: 1px solid rgba(255,255,255,0.10);
  }
  .jj-close h3 {
    margin: 0;
    font-size: clamp(24px, 3.2vw, 34px);
    font-weight: 780;
    letter-spacing: -0.028em;
    color: #FFFFFF;
  }
  .jj-close p { margin: 13px auto 0; max-width: 560px; font-size: 16px; line-height: 1.6; color: #A0B2D8; }
  .jj-close-row { display: flex; justify-content: center; flex-wrap: wrap; gap: 13px; margin-top: 30px; }
`}</style>

<div className="jj-wrap">

<div className="jj-nav">
  <a className="jj-nav-brand" href="https://json-jet.docs.buildwithfern.com/flight-status-api/introduction#json-jet-airways-flight-status-api">
    <span className="jj-nav-mark">JJ</span>
    <span className="jj-nav-name">JSON Jet Airways</span>
  </a>
  <nav className="jj-nav-links">
    <a className="jj-nav-link jj-nav-active" href="https://json-jet.docs.buildwithfern.com/flight-status-api/introduction#json-jet-airways-flight-status-api">Home</a>
    <a className="jj-nav-link" href="https://json-jet.docs.buildwithfern.com/flight-status-api/flight-status/get-flight-status">API Explorer</a>
    <a className="jj-nav-link" href="#">Blog</a>
    <a className="jj-nav-link" href="mailto:api@jsonjetairways.example">Contact</a>
    <a className="jj-nav-link jj-nav-cta" href="https://json-jet.docs.buildwithfern.com/flight-status-api/checkindemo">CheckIn Demo</a>
  </nav>
</div>

<div className="jj-hero">

<svg className="jj-orbit" viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <defs>
    <linearGradient id="jjOrbitA" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stopColor="#22D3EE" stopOpacity="0" />
      <stop offset="42%" stopColor="#7DD3FC" stopOpacity="0.85" />
      <stop offset="100%" stopColor="#F5B942" stopOpacity="0.55" />
    </linearGradient>
    <linearGradient id="jjOrbitB" x1="1" y1="0" x2="0" y2="1">
      <stop offset="0%" stopColor="#A5B4FC" stopOpacity="0.6" />
      <stop offset="100%" stopColor="#A5B4FC" stopOpacity="0" />
    </linearGradient>
    <radialGradient id="jjCore">
      <stop offset="0%" stopColor="#FFFFFF" />
      <stop offset="55%" stopColor="#7DD3FC" />
      <stop offset="100%" stopColor="#2B3FF3" stopOpacity="0" />
    </radialGradient>
    <filter id="jjSoft" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="5" result="b" />
      <feMerge><feMergeNode in="b" /><feMergeNode in="SourceGraphic" /></feMerge>
    </filter>
  </defs>

  <g fill="none" stroke="#FFFFFF" strokeOpacity="0.10">
    <circle cx="150" cy="150" r="128" />
    <circle cx="150" cy="150" r="94" strokeOpacity="0.07" />
    <circle cx="150" cy="150" r="58" strokeOpacity="0.05" />
  </g>

  <g stroke="#FFFFFF" strokeOpacity="0.14" strokeWidth="1">
    <path d="M150 8 V26 M150 274 V292 M8 150 H26 M274 150 H292" />
  </g>

  <g className="jj-orbit-spin">
    <g transform="rotate(-24 150 150)">
      <path id="jjPath" d="M22,150 A128,50 0 1,1 278,150 A128,50 0 1,1 22,150"
        fill="none" stroke="url(#jjOrbitA)" strokeWidth="1.6" filter="url(#jjSoft)" />
      <circle r="4.5" fill="#F5B942" filter="url(#jjSoft)">
        <animateMotion dur="11s" repeatCount="indefinite"
          path="M22,150 A128,50 0 1,1 278,150 A128,50 0 1,1 22,150" />
      </circle>
    </g>
    <g transform="rotate(46 150 150)">
      <path d="M46,150 A104,38 0 1,1 254,150 A104,38 0 1,1 46,150"
        fill="none" stroke="url(#jjOrbitB)" strokeWidth="1.2" opacity="0.7" />
      <circle r="3" fill="#7DD3FC" opacity="0.85">
        <animateMotion dur="17s" repeatCount="indefinite"
          path="M46,150 A104,38 0 1,1 254,150 A104,38 0 1,1 46,150" />
      </circle>
    </g>
  </g>

  <circle cx="150" cy="150" r="34" fill="url(#jjCore)" opacity="0.55" />
  <circle cx="150" cy="150" r="5" fill="#FFFFFF" filter="url(#jjSoft)" />
</svg>

<span className="jj-eyebrow">
  <span className="jj-dot" />
  JJ &middot; RESTown Hub &middot; API v1.0
</span>

<h1 className="jj-title">
  JSON Jet Airways<br />
  <span>Flight Status API</span>
</h1>

<p className="jj-tagline">
  Every gate change, every delay, every wheels-up — the moment it happens.
  One REST call, one JSON payload. <b>We wrap you in comfort {"{ }"}</b>
</p>

<div className="jj-cta-row">
  <a className="jj-btn jj-btn-primary" href="/flight-status-api/flight-status/get-flight-status">Try it in the API Explorer &rarr;</a>
  <a className="jj-btn jj-btn-ghost" href="#quickstart">Take off in 60 seconds</a>
  <a className="jj-btn jj-btn-ghost" href="https://www.postman.com/postman-italy-769381/workspace/public-json-jet-airways-we-wrap-you-in-comfort" target="_blank" rel="noopener noreferrer">Open the Postman workspace</a>
</div>

<div className="jj-chips">
  <span className="jj-chip">REST / JSON</span>
  <span className="jj-chip">GET /flight-status</span>
  <span className="jj-chip">x-api-key auth</span>
  <span className="jj-chip">ISO 8601 timestamps</span>
  <span className="jj-chip">Public sandbox</span>
</div>

</div>

<div className="jj-stats">
  <a
    className="jj-stat"
    href="https://statuspage.getpostman.com/1f192408-3f72-4350-9158-2c63cea01891"
    target="_blank"
    rel="noopener noreferrer"
    title="Live uptime monitor on Postman"
  >
    <div className="jj-stat-v">99.98%</div>
    <div className="jj-stat-l">Sandbox uptime</div>
  </a>
  <div className="jj-stat">
    <div className="jj-stat-v">&lt; 120ms</div>
    <div className="jj-stat-l">Median response</div>
  </div>
  <div className="jj-stat">
    <div className="jj-stat-v">1 call</div>
    <div className="jj-stat-l">To full flight state</div>
  </div>
  <div className="jj-stat">
    <div className="jj-stat-v">0 &euro;</div>
    <div className="jj-stat-l">To start building</div>
  </div>
</div>

<div className="jj-sec">

<div className="jj-kicker">Live from RESTown</div>
<h2 className="jj-h2">The board your users actually want</h2>
<p className="jj-lead">
  Each row below is a single <code>GET /flight-status</code> response. Gate, terminal, scheduled
  and estimated times, all normalised — so you can render a departures board, push a delay
  notification, or trigger a rebooking flow without stitching feeds together.
</p>
<p className="jj-lead" style={{ marginTop: "14px" }}>
  <a href="/flight-status-api/flight-status/get-flight-status">Open the full endpoint reference &rarr;</a>
</p>

<div className="jj-board">
  <div className="jj-board-top">
    <div className="jj-board-title">JSON Jet Airways &middot; Departures</div>
    <div className="jj-live"><span className="jj-dot" /> LIVE FEED</div>
  </div>

  <div className="jj-row jj-row-head">
    <div>Flight</div><div>Route</div><div>Scheduled</div><div>Gate</div><div>Status</div>
  </div>

  <div className="jj-row">
    <div className="jj-flt">JJ 202</div>
    <div className="jj-route">JFK <em>&rarr;</em> LAX</div>
    <div>14:30Z</div>
    <div>B12</div>
    <div><span className="jj-pill jj-delayed">DELAYED</span></div>
  </div>

  <div className="jj-row">
    <div className="jj-flt">JJ 118</div>
    <div className="jj-route">FRA <em>&rarr;</em> LHR</div>
    <div>09:05Z</div>
    <div>A04</div>
    <div><span className="jj-pill jj-ontime">ON_TIME</span></div>
  </div>

  <div className="jj-row">
    <div className="jj-flt">JJ 350</div>
    <div className="jj-route">CDG <em>&rarr;</em> AMS</div>
    <div>11:20Z</div>
    <div>D22</div>
    <div><span className="jj-pill jj-boarding">BOARDING</span></div>
  </div>

  <div className="jj-row">
    <div className="jj-flt">JJ 771</div>
    <div className="jj-route">MAD <em>&rarr;</em> ORD</div>
    <div>06:45Z</div>
    <div>C08</div>
    <div><span className="jj-pill jj-landed">LANDED</span></div>
  </div>

  <div className="jj-row">
    <div className="jj-flt">JJ 909</div>
    <div className="jj-route">AMS <em>&rarr;</em> JFK</div>
    <div>17:10Z</div>
    <div>&mdash;</div>
    <div><span className="jj-pill jj-cancel">CANCELLED</span></div>
  </div>
</div>

</div>

<div className="jj-sec" id="quickstart">

<div className="jj-kicker">Quickstart</div>
<h2 className="jj-h2">Wheels up in three moves</h2>
<p className="jj-lead">
  No SDK, no onboarding call, no contract. Point at the sandbox, send your carrier code and
  flight number, and read the JSON.
</p>

</div>

<Steps>

<Step title="Get your key">

Fork the [public Postman workspace](https://www.postman.com/postman-italy-769381/workspace/public-json-jet-airways-we-wrap-you-in-comfort), select the **Production** environment, and drop your key into the `apiKey` variable. Every request authenticates with an `x-api-key` header.

</Step>

<Step title="Call the endpoint">

One `GET`, two query parameters. You can also [run this live in the API Explorer](/flight-status-api/flight-status/get-flight-status) — paste your key into the Authorization field and hit Send.

<CodeBlocks>

```bash title="cURL"
curl -sS "https://fef3af9d-2af6-417d-9153-30ea3eb9ad46.mock.pstmn.io/flight-status?carrierCode=JJ&flightNumber=202" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Accept: application/json"
```

```javascript title="TypeScript"
const params = new URLSearchParams({ carrierCode: "JJ", flightNumber: "202" });

const res = await fetch(
  `https://fef3af9d-2af6-417d-9153-30ea3eb9ad46.mock.pstmn.io/flight-status?${params}`,
  { headers: { "x-api-key": process.env.JJ_API_KEY, Accept: "application/json" } }
);

const flight = await res.json();
console.log(`${flight.carrierCode}${flight.flightNumber} is ${flight.status}`);
```

```python title="Python"
import os, requests

res = requests.get(
    "https://fef3af9d-2af6-417d-9153-30ea3eb9ad46.mock.pstmn.io/flight-status",
    params={"carrierCode": "JJ", "flightNumber": "202"},
    headers={"x-api-key": os.environ["JJ_API_KEY"], "Accept": "application/json"},
)

flight = res.json()
print(f"{flight['carrierCode']}{flight['flightNumber']} is {flight['status']}")
```

</CodeBlocks>

</Step>

<Step title="Read the cabin">

`200 OK` returns the complete flight state — nothing else to fetch.

```json title="200 OK"
{
  "carrierCode": "JJ",
  "flightNumber": "202",
  "status": "DELAYED",
  "departureAirport": "JFK",
  "arrivalAirport": "LAX",
  "scheduledDeparture": "2026-07-28T14:30:00Z",
  "estimatedDeparture": "2026-07-28T15:10:00Z",
  "scheduledArrival": "2026-07-28T17:45:00Z",
  "estimatedArrival": "2026-07-28T18:25:00Z",
  "gate": "B12",
  "terminal": "4",
  "lastUpdated": "2026-07-28T13:05:00Z"
}
```

</Step>

</Steps>

<div className="jj-wrap" style={{ padding: 0, maxWidth: "100%" }}>

<div className="jj-sec">

<div className="jj-kicker">Network</div>
<h2 className="jj-h2">Eight hubs, one contract</h2>
<p className="jj-lead">
  The sandbox mirrors our transatlantic core. Same schema on every route, every cabin,
  every carrier code you throw at it.
</p>

<div className="jj-map">
<svg viewBox="0 0 1000 400" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="JSON Jet Airways route network across eight hubs">
  <defs>
    <linearGradient id="jjArc" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stopColor="#22D3EE" stopOpacity="0.15" />
      <stop offset="50%" stopColor="#7DD3FC" stopOpacity="0.95" />
      <stop offset="100%" stopColor="#F5B942" stopOpacity="0.85" />
    </linearGradient>
    <radialGradient id="jjNode">
      <stop offset="0%" stopColor="#FFFFFF" />
      <stop offset="100%" stopColor="#7DD3FC" />
    </radialGradient>
    <filter id="jjGlow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="7" result="b" />
      <feMerge><feMergeNode in="b" /><feMergeNode in="SourceGraphic" /></feMerge>
    </filter>
  </defs>

  <g stroke="#FFFFFF" strokeOpacity="0.045" strokeWidth="1">
    <path d="M0 80 H1000 M0 160 H1000 M0 240 H1000 M0 320 H1000" />
    <path d="M125 0 V400 M250 0 V400 M375 0 V400 M500 0 V400 M625 0 V400 M750 0 V400 M875 0 V400" />
  </g>

  <g fill="none" strokeLinecap="round">
    <path d="M150 268 Q290 168 340 214" stroke="url(#jjArc)" strokeWidth="2" opacity="0.5" className="jj-dash" />
    <path d="M340 214 Q470 96 640 158" stroke="url(#jjArc)" strokeWidth="3.2" filter="url(#jjGlow)" id="jjMain" />
    <path d="M262 196 Q460 108 726 172" stroke="url(#jjArc)" strokeWidth="2" opacity="0.55" className="jj-dash" />
    <path d="M640 158 Q660 200 622 246" stroke="url(#jjArc)" strokeWidth="1.8" opacity="0.45" className="jj-dash" />
    <path d="M666 148 Q700 130 726 172" stroke="url(#jjArc)" strokeWidth="1.8" opacity="0.45" className="jj-dash" />
    <path d="M150 268 Q210 216 262 196" stroke="url(#jjArc)" strokeWidth="1.8" opacity="0.4" className="jj-dash" />
    <path d="M622 246 Q640 300 726 172" stroke="url(#jjArc)" strokeWidth="1.5" opacity="0.3" className="jj-dash" />
  </g>

  <g>
    <path d="M0,-6 L19,0 L0,6 L5.5,0 Z" fill="#F5B942">
      <animateMotion dur="7s" repeatCount="indefinite" rotate="auto"
        path="M340 214 Q470 96 640 158" />
    </path>
  </g>

  <g fontFamily="ui-monospace, SFMono-Regular, Menlo, monospace" fontSize="13" fontWeight="700" letterSpacing="1.4">
    <g>
      <circle cx="150" cy="268" r="6" fill="url(#jjNode)" filter="url(#jjGlow)" />
      <text x="150" y="296" fill="#B6C7EA" textAnchor="middle">LAX</text>
    </g>
    <g>
      <circle cx="262" cy="196" r="5" fill="url(#jjNode)" />
      <text x="262" y="178" fill="#8FA2C9" textAnchor="middle">ORD</text>
    </g>
    <g>
      <circle cx="340" cy="214" r="7" fill="url(#jjNode)" filter="url(#jjGlow)" />
      <text x="340" y="242" fill="#B6C7EA" textAnchor="middle">JFK</text>
    </g>
    <g>
      <circle cx="640" cy="158" r="7" fill="url(#jjNode)" filter="url(#jjGlow)" />
      <text x="640" y="140" fill="#B6C7EA" textAnchor="middle">LHR</text>
    </g>
    <g>
      <circle cx="666" cy="148" r="4" fill="#7DD3FC" />
      <text x="700" y="118" fill="#8FA2C9" textAnchor="middle">AMS</text>
    </g>
    <g>
      <circle cx="622" cy="246" r="5" fill="url(#jjNode)" />
      <text x="600" y="274" fill="#8FA2C9" textAnchor="middle">CDG</text>
    </g>
    <g>
      <circle cx="726" cy="172" r="6" fill="url(#jjNode)" filter="url(#jjGlow)" />
      <text x="760" y="166" fill="#B6C7EA" textAnchor="middle">FRA</text>
    </g>
    <g>
      <circle cx="560" cy="292" r="4" fill="#7DD3FC" />
      <text x="560" y="318" fill="#8FA2C9" textAnchor="middle">MAD</text>
    </g>
  </g>

  <text x="928" y="376" fill="#F5B942" fontFamily="ui-monospace, SFMono-Regular, Menlo, monospace" fontSize="11" letterSpacing="2.4" textAnchor="end" opacity="0.75">JJ NETWORK</text>
</svg>
</div>

</div>

<div className="jj-sec">

<div className="jj-kicker">Response schema</div>
<h2 className="jj-h2">Thirteen fields. No surprises.</h2>
<p className="jj-lead">
  Flat, predictable, and stable. Timestamps are always ISO 8601 in UTC, airports are always
  IATA codes, and every field is present on a <code>200</code>.
</p>

<div className="jj-grid">
  <div className="jj-field">
    <div className="jj-field-k">carrierCode</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Two-letter IATA carrier code. <code>JJ</code> for JSON Jet Airways.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">flightNumber</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Numeric flight number without the carrier prefix, 1&ndash;4 digits.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">status</div>
    <div className="jj-field-t">enum</div>
    <div className="jj-field-d">One of six lifecycle states, from <code>ON_TIME</code> to <code>CANCELLED</code>.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">departureAirport</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Origin IATA code, for example <code>JFK</code>.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">arrivalAirport</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Destination IATA code, for example <code>LAX</code>.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">scheduledDeparture</div>
    <div className="jj-field-t">date-time</div>
    <div className="jj-field-d">Published departure time as filed, in UTC.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">estimatedDeparture</div>
    <div className="jj-field-t">date-time</div>
    <div className="jj-field-d">Current best estimate. Diverges from scheduled when delayed.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">scheduledArrival</div>
    <div className="jj-field-t">date-time</div>
    <div className="jj-field-d">Published arrival time as filed, in UTC.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">estimatedArrival</div>
    <div className="jj-field-t">date-time</div>
    <div className="jj-field-d">Live arrival estimate, updated as the flight progresses.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">gate</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Assigned gate, for example <code>B12</code>. Empty until allocated.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">terminal</div>
    <div className="jj-field-t">string</div>
    <div className="jj-field-d">Departure terminal at the origin airport.</div>
  </div>
  <div className="jj-field">
    <div className="jj-field-k">lastUpdated</div>
    <div className="jj-field-t">date-time</div>
    <div className="jj-field-d">When this record was last refreshed. Use it to age your cache.</div>
  </div>
</div>

</div>

</div>

## Flight lifecycle

The `status` field moves through six states. Model your UI against these and nothing else.

<div className="jj-tablewrap">
<table className="jj-table">
<thead>
<tr>
<th>Status</th>
<th>Meaning</th>
<th>Typical UI treatment</th>
</tr>
</thead>
<tbody>
<tr>
<td><span className="jj-pill jj-ontime">ON_TIME</span></td>
<td>Operating to schedule</td>
<td><span className="jj-swatch jj-sw-green" />No action needed</td>
</tr>
<tr>
<td><span className="jj-pill jj-boarding">BOARDING</span></td>
<td>Gate is open, doors close soon</td>
<td><span className="jj-swatch jj-sw-amber" />Surface the gate prominently</td>
</tr>
<tr>
<td><span className="jj-pill jj-delayed">DELAYED</span></td>
<td>Estimated times have moved</td>
<td><span className="jj-swatch jj-sw-amber" />Show the delta and the new estimate</td>
</tr>
<tr>
<td><span className="jj-pill jj-landed">DEPARTED</span></td>
<td>Wheels up</td>
<td><span className="jj-swatch jj-sw-neutral" />Switch to arrival countdown</td>
</tr>
<tr>
<td><span className="jj-pill jj-landed">LANDED</span></td>
<td>On the ground at destination</td>
<td><span className="jj-swatch jj-sw-neutral" />Surface baggage information</td>
</tr>
<tr>
<td><span className="jj-pill jj-cancel">CANCELLED</span></td>
<td>Flight will not operate</td>
<td><span className="jj-swatch jj-sw-red" />Trigger your rebooking flow</td>
</tr>
</tbody>
</table>
</div>

<Callout intent="tip" title="Test every branch without leaving the sandbox">
The public mock is deterministic, so you can drive your error states on demand:
`flightNumber=404` returns a `FLIGHT_NOT_FOUND`, and any non-numeric value such as
`flightNumber=abc` returns an `INVALID_FLIGHT_NUMBER`. Everything else returns a healthy `200`.
</Callout>

## Error handling

Errors are machine-first: a stable `code` you can branch on, plus a human-readable `message` you can log.

<AccordionGroup>

<Accordion title="400 · INVALID_FLIGHT_NUMBER">

Triggered when `flightNumber` contains anything other than digits. Validate against `^[0-9]{1,4}$` before you call.

```json title="400 Bad Request"
{
  "code": "INVALID_FLIGHT_NUMBER",
  "message": "Flight number must consist of numbers only. Expected pattern: ^[0-9]{1,4}$"
}
```

</Accordion>

<Accordion title="404 · FLIGHT_NOT_FOUND">

The carrier and flight number are well-formed, but no flight matches. Treat as an empty result, not a failure — flight numbers retire and rotate.

```json title="404 Not Found"
{
  "code": "FLIGHT_NOT_FOUND",
  "message": "No flight found for carrier JJ, flight number 404."
}
```

</Accordion>

</AccordionGroup>

## Where to next

<CardGroup cols={3}>

<Card
  title="Postman workspace"
  icon="regular rocket-launch"
  href="https://www.postman.com/postman-italy-769381/workspace/public-json-jet-airways-we-wrap-you-in-comfort"
>
  Fork the collection, switch to the Production environment, and send your first request in the browser.
</Card>

<Card
  title="API reference"
  icon="regular plane-departure"
  href="/flight-status-api/flight-status/get-flight-status"
>
  Full parameter list, response schemas, and a live Explorer for GET /flight-status.
</Card>

<Card
  title="Talk to the crew"
  icon="regular envelope"
  href="mailto:api@jsonjetairways.example"
>
  Integration questions, partner access, or higher rate limits — the API team answers directly.
</Card>

</CardGroup>

<div className="jj-wrap" style={{ padding: 0, maxWidth: "100%" }}>

<div className="jj-close">
  <h3>Comfort is a contract we keep.</h3>
  <p>
    Stable schemas, honest status codes, and a sandbox that behaves exactly like production.
    That is what wrapping you in comfort actually means.
  </p>
  <div className="jj-close-row">
    <a className="jj-btn jj-btn-primary" href="/flight-status-api/flight-status/get-flight-status">Open the API reference &rarr;</a>
    <a className="jj-btn jj-btn-ghost" href="mailto:api@jsonjetairways.example">Contact the API team</a>
  </div>
</div>

</div>

</div>
